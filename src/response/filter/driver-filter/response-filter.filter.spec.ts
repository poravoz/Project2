import { ResponseFilterFilter } from './response-filter.filter';


describe('DriverFilterFilter', () => {
  it('should be defined', () => {
    expect(new ResponseFilterFilter()).toBeDefined();
  });
});
